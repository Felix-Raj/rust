fn main() {
    /*
        Packages: A Cargo feature that lets you build, test, and share crates
                is one or more crates
                Cargo.toml describes how to build this crates
                Must contain 0 or one library crate and no more, can contain any number of binary
                    crate
                By default *crate root* is src/main.rs
                A package can have multiple binary crates by placing files in
                    the src/bin directory: each file will be a separate binary crate.
        Crates: A tree of modules that produces a library or executable
        Modules and use: Let you control the organization, scope, and privacy of paths
        Paths: A way of naming an item, such as a struct, function, or module
    */
}